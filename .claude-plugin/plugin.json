{
  "name": "token-telemetry",
  "version": "0.4.0",
  "description": "Zero-token-overhead token usage telemetry: Stop/SubagentStop hooks record per-turn usage into a central SQLite DB; /token-stats reports; /storage-* manages where the data lives.",
  "author": { "name": "Zenosyne" }
}
