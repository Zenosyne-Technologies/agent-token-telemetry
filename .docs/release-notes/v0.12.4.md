# token-telemetry v0.12.4

**Date:** 2026-09-02 · **Tag:** `v0.12.4` (also back-tags `v0.12.3` at its merge)

## Frozen scope

Issue-key set for this version (the version's cost rollup per `docs/TELEMETRY-CONTRACT.md` → Scoping recipes: `report.py --scope AOS-72,AOS-79`):

- **AOS-72** (v0.12.3) — project-name lookup resolves `PROJECT-INFO.md` via the `.marvin/` → `.docs/` → `docs/` ladder; fresh enrollments and renames self-heal after a kit ≥v0.21 upgrade.
- **AOS-79** (v0.12.4) — milestone-branch rollup (`branch LIKE 'milestone/%'`) removed; `--scope KEY1,KEY2` issue-key-set rollup with subject-line commit fallback and three distinguishable empty states (scope resolution failed / telemetry absent / 0-of-N broken scope). `events.branch` is corroboration only, never a grouping key.

## Coordination

Pair this release with **kit v0.22.0**: both fixes exist to close the upgrade gap (PROJECT-INFO path move, gitflow branch scheme). A repo upgraded to kit v0.22.0 with telemetry <0.12.4 loses name auto-sync and reads milestone cost as a silent zero.

## Verification

197/197 tests. Both changes passed fresh completion validation at the real boundary (hook pipe / CLI renders) and security validation (AOS-79 required one fix round: commit-sha fallback constrained to subject-line `KEY:` match; rejected-scope-key echo sanitized). Deferred hardening tracked: AOS-90, AOS-91, AOS-10.
