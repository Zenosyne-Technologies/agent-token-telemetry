# token-telemetry v0.12.5

**Date:** 2026-09-02 · **Tag:** `v0.12.5`

## Frozen scope

Issue-key set for this version (`report.py --scope AOS-90,AOS-91`):

- **AOS-90** — PROJECT-INFO ladder reads resolve-and-contain to the repo root: an outside-root symlink at any ladder location yields no name at that location (no fall-through); in-root symlinks and symlinked roots (macOS `/tmp`) keep working.
- **AOS-91** — `md_cell()` sanitizes untrusted names for markdown output: `|` escaped, backticks replaced, `\r\n\t` + U+2028/U+2029 folded to spaces, C0 controls + DEL and bidi controls (U+202A–202E, U+2066–2069) stripped, leading markdown punctuation stripped, 120-char cap. Applied to every name-like cell in `report.py`.

Both originate from the AOS-72 security validation (hardening backlog).

## Verification

202/202 tests. Completion validation at the real capture/CLI boundary (one fix round: C0 control bytes); security validation PASS (one fix round: bidi controls). Accepted cosmetic residuals: stacked combining marks can render tall glyphs; an all-control-character name renders as a blank cell.
